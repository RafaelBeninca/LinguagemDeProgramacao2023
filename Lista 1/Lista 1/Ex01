#Faça um script que leia dois números e retorne como resultado a soma deles. Faça um script que leia algo
#pelo teclado e mostra na tela o seu tipo de dado.
# 1 a)
a = int(input("Digite um número: "))
b = int(input("Digite outro número: "))

print(a + b)


# 1 b)
# Explicação:

# O valor inserido seria sempre a mesma variável independentemente do valor inserido, pois o tipo de valor
# já é aplicado ao digitar o valor.